## Scss/

## Dir. structure

```
scss/
  - base - Contains reference base implementation (css reset, + base html styles).
  - functions
  - mixins
  - placeholders
  - themes - Contains theme variable definitions (uses/extends variables).
  - variables - Contains all scss variables in './scss' directory.
```
